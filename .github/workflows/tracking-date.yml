name: "Auto Manage Tracking Date"

on:
  workflow_dispatch:

permissions:
  issues: write
  
jobs:
  call-tracking-date:
    uses: JamesonRGrieve/Workflows/.github/workflows/tracking-date-reusuable.yml@main
    ###Tracking Dates Reusable Workflow###